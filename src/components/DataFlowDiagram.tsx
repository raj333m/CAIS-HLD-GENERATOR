'use client';

import React from 'react';

// Diagrams are explicitly out of scope for this pass and must not be rendered anywhere
export default function DataFlowDiagram(props?: any) {
  return null;
}
